checkout:
  post:
    - sudo apt-get install -y autopoint automake-1.15 gettext libreadline-dev wget texinfo
    - sh ./autogen.sh
    - sh ./configure --enable-maintainer-mode
    - make

test:
  override:
    - make check
    - make distcheck
