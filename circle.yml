checkout:
  post:
    - sudo apt-get install -y autopoint gettext libreadline-dev wget texinfo
    - sh ./autogen.sh
    - sh ./configure --enable-maintainer-mode
    - make

test:
  override:
    - make check
    - make distcheck
