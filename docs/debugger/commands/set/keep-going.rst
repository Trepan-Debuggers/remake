.. index:: set; keep-going
.. _set_keep-going:

Set GNU Make `-k` flag (`set keep-going`)
-----------------------------------------

**set keep-going** [ **on** | **off** | **toggle** ]

Set value of GNU Make `--keep-going` (or `-k`) flag.
