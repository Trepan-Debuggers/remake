.. index:: set; silent
.. _set_silent:

Set GNU Make `-s` Flag (`set silent`)
-------------------------------------

**set silent** [ **on** | **off** | **toggle** ]

Set value of GNU Make `--silent` (or `-s`) flags.
