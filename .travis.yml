language: C

sudo: false

install:
 - sudo apt-get install  -y autopoint gettext libreadline-dev wget texinfo

# Configure and run the tests
script:
 - autoreconf -i && ./configure --enable-maintainer-mode && make update
 - (cd doc && make stamp-vti) && make && TRAVIS=1 make check
